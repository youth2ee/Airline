<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.airline.a1.member.MemberMapper">
	<select id="getBookingList" parameterType="com.airline.a1.checkIn.ETicketVO" resultType="com.airline.a1.checkIn.ETicketVO">
		select * from bookingTicket where id=#{id};
	</select>
	<select id="getBookingMore" parameterType="com.airline.a1.checkIn.ETicketVO" resultType="com.airline.a1.checkIn.ETicketVO">
		select fnum,bookingNum,flightBNum,name,id,depAirportNm,vihicleId,arrAirportNm,depPlandTime,arrPlandTime from flightData join bookingTicket on bookingTicket.depFnum = flightData.fnum where bookingNum = #{bookingNum};
	</select>
  </mapper>
